{% extends 'base.html.twig' %}

{% block title %}Признак{% endblock %}

{% block body %}
    <h1>Признак</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prizn.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ prizn.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prizn_index') }}">Назад к списку</a>

    <a href="{{ path('prizn_edit', {'id': prizn.id}) }}">Изменить</a>

    {{ include('prizn/_delete_form.html.twig') }}
{% endblock %}
