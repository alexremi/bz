{% extends 'base.html.twig' %}

{% block title %}Признак{% endblock %}

{% block body %}
    <h1>Признак</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prizn.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ prizn.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prizn_index') }}">back to list</a>

    <a href="{{ path('prizn_edit', {'id': prizn.id}) }}">edit</a>

    {{ include('prizn/_delete_form.html.twig') }}
{% endblock %}
