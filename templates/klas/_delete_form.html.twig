<form method="post" action="{{ path('klas_delete', {'id': kla.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить эту запись?');">
    <input type="hidden" name="_method" value="Удалить">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kla.id) }}">
    <button class="btn">Удалить</button>
</form>
