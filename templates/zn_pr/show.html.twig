{% extends 'base.html.twig' %}

{% block title %}Значение для прзнака{% endblock %}

{% block body %}
    <h1>Значение для признака</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zn_pr.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ zn_pr.priznaArea }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zn_pr_index') }}">Назад к списку</a>

    <a href="{{ path('zn_pr_edit', {'id': zn_pr.id}) }}">Изменить</a>

    {{ include('zn_pr/_delete_form.html.twig') }}
{% endblock %}
