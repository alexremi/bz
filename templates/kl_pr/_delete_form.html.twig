<form method="post" action="{{ path('kl_pr_delete', {'id': kl_pr.id}) }}" onsubmit="return confirm('Вы действительно хотит удалить эту запись?');">
    <input type="hidden" name="_method" value="Удалить">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kl_pr.id) }}">
    <button class="btn">Удалить</button>
</form>
