{% extends 'base.html.twig' %}

{% block title %}KlPr{% endblock %}

{% block body %}
    <h1>KlPr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kl_pr.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kl_pr_index') }}">back to list</a>

    <a href="{{ path('kl_pr_edit', {'id': kl_pr.id}) }}">edit</a>

    {{ include('kl_pr/_delete_form.html.twig') }}
{% endblock %}
